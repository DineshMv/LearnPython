# Modules are just .py scripts that can be called in another .py script
# Packages are a collection of modules.
# Each package should have a __init__.py file which can have any statements, functions or can be empty
# Each package can have multiple subpackages.

def my_func():
    print(f'Hey, I am a python Module')